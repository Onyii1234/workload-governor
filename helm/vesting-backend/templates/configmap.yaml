apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vesting-backend.fullname" . }}-config
  labels:
    {{- include "vesting-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
